{
  "scripts": {
    "check": {
      "desc": "Type-checks the code",
      "cmd": "deno run -c Deno.jsonc mod.ts"
    },
    "fmt": {
      "desc": "Formats code",
      "cmd": "deno fmt -c Deno.jsonc"
    },
    "lint": {
      "desc": "Lints code",
      "cmd": "deno lint -c Deno.jsonc"
    },
    "pre-commit": {
      "cmd": [
        "vr fmt",
        "vr lint",
        "vr check"
      ],
      "gitHook": "pre-commit"
    }
  }
}
